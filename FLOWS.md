# Cyprus Cleaners — Complete Flow Reference

Exhaustive map of every screen, route, state transition, email, and gap in the app, traced directly from the codebase (routes under `src/app/[locale]/` and `src/app/api/`, schema in `supabase/schema.sql`, types in `src/types/index.ts`). Where the code is ambiguous or untested rather than the doc being incomplete, that's called out explicitly rather than guessed at.

File refs are `path:line` — anchors as of this writing, not permanent.

---

## Table of contents

1. [Every screen](#1-every-screen)
2. [End-to-end user flows by role](#2-end-to-end-user-flows-by-role)
3. [The booking lifecycle](#3-the-booking-lifecycle)
4. [The payment flow](#4-the-payment-flow)
5. [The email map](#5-the-email-map)
6. [Known gaps and half-built flows](#6-known-gaps-and-half-built-flows)
7. [Admin flows](#7-admin-flows)
8. [Cross-party interactions](#8-cross-party-interactions)
9. [Authentication and access control](#9-authentication-and-access-control)
10. [Edge cases and race conditions](#10-edge-cases-and-race-conditions)
11. [Data model reference](#11-data-model-reference)

---

## 1. Every screen

Every `page.tsx` under `src/app/[locale]/`, in the order a user would typically meet them. "Access" is the enforced rule — middleware where noted, otherwise a client-side `useSession` guard (belt-and-suspenders, not the real boundary).

File paths below sit under one of three route groups — `(marketing)/`, `(auth)/`, or `(app)/` — which only organize files, not URLs (a route group's parentheses never appear in the actual path, so every URL in the table below is unchanged). `(marketing)/layout.tsx` renders Navbar+Footer; `(auth)/layout.tsx` is a minimal logo-only header (no Navbar/Footer, to reduce signup drop-off); `(app)/layout.tsx` renders Navbar (desktop only, mobile hidden when logged in) and is the one place the per-page auth/role redirect guard now lives (see below).

- **`BottomTabBar` is global, not route-group-scoped (settled 2026-08-19, "Option A").** It's rendered once in the shared `src/app/[locale]/layout.tsx` (inside `Providers`, alongside every route group), not inside `(app)/layout.tsx`. It self-gates on `session.user.role` (renders `null` unless `CUSTOMER`/`CLEANER`), so it shows for a logged-in user on *any* route — marketing pages included — not just inside the `(app)` shell. This replaces two earlier, narrower fixes that both turned out to be papering over the same underlying issue: (a) 2026-08-18's `hideOnMobileWhenLoggedIn` prop on `Navbar`, added when `BottomTabBar` was first scoped to `(app)` only and marketing pages briefly had no mobile nav at all for a logged-in visitor; (b) the scoping itself, which caused the actual reported bug — a logged-in user tapping their own bottom nav's "Home" or "Search" got routed to the marketing site and *out* of the app shell, since those tabs pointed at `/` and `/cleaners` (both marketing routes) while `BottomTabBar` only rendered inside `(app)`. Both `Navbar` usages now pass `hideOnMobileWhenLoggedIn` unconditionally — safe everywhere now that `BottomTabBar` actually is global again — and clearance for the fixed bar is handled once, globally: `BottomTabBar` renders a normal-flow spacer div as the last element in `<body>` (same session-role gate as the bar itself), instead of the old per-page opt-in `.pb-tabbar` CSS class (removed).
- **Tab destinations, for a logged-in user**: Search → `/dashboard/search` (new page, added 2026-08-19 — an app-shell-wrapped copy of the public directory, see below, `CUSTOMER`-only) · Profile → `/dashboard/profile`, same for both roles.
  - **`CUSTOMER`**: Home → `/dashboard`, Bookings → `/dashboard/bookings`, Messages → `/dashboard/messages` — three **real, distinct routes** as of 2026-08-21.
  - **`CLEANER`**: Home → `/dashboard/cleaner`, Bookings → `/dashboard/cleaner/bookings`, Messages → `/dashboard/cleaner/messages` — same treatment, same day.
  - Both used to be a single page with Home aliased to Bookings (Home's bare URL fell through to the same `'bookings'` `?tab=` default) and every banner rendering unconditionally above the tab switch, leaking into Messages too. See "Cleaner dashboard IA refactor" below — built for `CLEANER` first, then the identical treatment applied to `CUSTOMER` the same day, per Sasha's decision to split the work into two passes.
- **`/dashboard/search`** (added 2026-08-19): the app-native counterpart to the public `/cleaners` directory, reached from `BottomTabBar`'s Search tab. Renders the exact same `CleanersDirectoryView` component (moved to `src/components/cleaners/CleanersDirectoryView.tsx` so both routes can import it — it was previously colocated under `(marketing)/cleaners/`) inside the `(app)` shell instead of marketing chrome, so a logged-in user browsing/team-selecting cleaners never leaves the app's own nav. No middleware change was needed — it's covered by the existing `pathname.startsWith('/dashboard')` rule (any authenticated user). **Search tab is `CUSTOMER`-only as of 2026-08-21** — `BottomTabBar`'s tab list is now built conditionally on `role`, matching the desktop Navbar (which already excluded "Find a cleaner" for `CLEANER`); a cleaner's tab bar is Home/Bookings/Messages/Profile, no Search.
- **Authenticated-app-shell escape hatches closed, 2026-08-21** (following the 2026-08-20 nav/routing investigation, Todoist): the marketing homepage becoming logged-out-only (see the `/` row above) meant every hardcoded `href="/"` needed to become role-aware instead. A shared `useHomeHref()` hook (`src/hooks/useHomeHref.ts`) now backs the Navbar logo, the 404 page, and the `CleanersDirectoryView`/`CleanerProfileView` breadcrumbs (shared between the marketing and app-native routes) — all four previously sent a logged-in visitor back to the marketing page. `Footer` gained the same `hideOnMobileWhenLoggedIn` prop `Navbar` already had, so it no longer renders under a logged-in visitor's mobile view of `/cleaners/[slug]` (marketing chrome leaking into what should read as an app-native page one tap before the profile page below existed). Not done in this pass, deliberately scoped out: the other marketing pages (`/cleaners`, `/for-cleaners`, `/contact`, `/privacy`, `/terms`) are untouched, and a direct/bookmarked URL to `/cleaners/[slug]` still shows marketing chrome even when logged in — only in-app navigation (card clicks, breadcrumbs, the nav link) routes to the app-native page.
- **Dashboard IA refactor — 2026-08-21** (Todoist, Sasha's spec in chat, built for `CLEANER` first then `CUSTOMER` the same day): both single `?tab=` pages had Home aliased to Bookings and every banner rendering unconditionally above the tab switch, leaking into Messages too. Split into three real routes per role, each fetching only its own data (no more fetching bookings+intros+disputes+earnings+email-verified on every load regardless of which tab was showing):
  - **Home** (`/dashboard/cleaner`, `/dashboard`): notification banners, the welcome heading, and **upcoming-only** confirmed bookings, soonest-first, no cap, no history.
    - `CLEANER`'s banners: email verify, disputes, a "new booking requests" banner (count + link to Bookings — REQUESTED bookings' actionable cards stay on Bookings only), payout, profile-incomplete, verification status, and the referral/invite-a-cleaner card.
    - `CUSTOMER`'s banners: email verify, an "awaiting confirmation" banner (informational — the customer isn't the one confirming, unlike the cleaner's equivalent), and a "reviews needed" banner (completed jobs with no review yet — the actual `ReviewPrompt` UI still lives on Bookings under the relevant History card; Home's version is just the notification).
  - **Bookings** (`/dashboard/cleaner/bookings`, `/dashboard/bookings`): the full needs-response-or-awaiting-confirmation/upcoming/history groups, unchanged content from the old page — no banners, no welcome heading, no invite card.
  - **Messages** (`/dashboard/cleaner/messages`, `/dashboard/messages`): just the thread list + `ChatPanel` — no banners. `CUSTOMER`'s version additionally reads `?open=<introductionId>` to auto-expand a thread, replacing what "Book again" (`BookingDetailModal`'s CTA, viewed from either Home or Bookings) used to do by staying on the same page and calling `setOpenChatId` directly — now it's a real cross-page navigation (`router.push('/dashboard/messages?open=...')`) instead.
  - Shared logic extracted per role rather than copy-pasted three ways: `useCleanerBookingActions()` / `useCustomerBookingActions()` (`src/hooks/`) each hold their role's bookings fetch + every action handler (`CLEANER`: confirm/decline/complete, contest/corroborate no-show, recurring skip/cancel, photo upload; `CUSTOMER`: cancel, file dispute, file no-show, recurring skip/cancel, review skip-tracking); `CleanerBookingCard` / `CustomerBookingCard` (`src/components/dashboard/`) are the card UI, taking the matching hook's return value as props. Home (upcoming only) and Bookings (all groups) render the same card component against their own independent hook instance per page — no shared cache, matching this codebase's existing per-page-fetch convention.
  - **Referral/invite-a-cleaner card (`CLEANER` only) now actually gates on the badge**: previously showed forever once `referral_code` existed (always true post-backfill), never checking whether `referred_friend` had already been earned — no client-facing read of a cleaner's own badges existed at all before this. `GET /api/cleaner-profiles/me` now also returns `earned_badge_keys` (deduped `cleaner_badges.badge_key` list, admin client — no RLS concerns).
  - **Routing choice**: real Next.js routes over the `?tab=` pattern, deliberately, for both roles — per-route code-splitting means Messages no longer ships the dispute/no-show/recurring-series/photo-upload JS it never uses, and each route fetches only what it needs.
  - **Desktop nav**: `Navbar`'s `navLinks` for both roles now includes Home/Bookings/Messages as an equal-weight set (plus Profile, rendered separately as it always was, and — `CUSTOMER` only — "Find a cleaner" positioned like the bottom tab bar's Search tab, between Home and Bookings). Neither role has a separately-styled ghost button any more (`ghostBtn` is `null` for both) — Home used to be that ghost button (labelled "Dashboard", later "Home" for `CLEANER` mid-refactor) and read as a special CTA distinct from Bookings/Messages/Profile; now all of them share the same plain nav-link treatment. `ADMIN` is unchanged, still has its own "Dashboard" ghost button — that panel hasn't been restructured this way. Earnings (`CLEANER` only) — previously not linked from anywhere in the new nav, only reachable via the Home payout banner or a direct URL — now has a permanent entry on `/dashboard/profile` (`t('earningsTab')`, between Edit profile and View public profile), settings-adjacent rather than a 5th top-level tab, per Sasha's decision.

| Route | Renders | Access | Key actions | Leads to |
|---|---|---|---|---|
| `/` | Home — hero, featured cleaners, footer | **Logged-out only as of 2026-08-21** — an authenticated visitor is redirected (middleware, via `getToken`) to their own role's app home (`homeForRole()`: `ADMIN → /admin`, `CLEANER → /dashboard/cleaner`, else `/dashboard`) before the marketing page ever renders | Browse cleaners, get started | `/cleaners`, `/get-started` |
| `/get-started` | Role picker — "I need a cleaner" / "I'm a cleaner" | Public | Pick a path | `/register`, `/register/cleaner` |
| `/for-cleaners` | Marketing/recruitment page for prospective cleaners | Public | CTA to register | `/register/cleaner` |
| `/privacy` | Static privacy policy | Public | — | — |
| `/register` | Customer signup form | Public | Submit form | Auto signs in → `/dashboard` |
| `/register/cleaner` | Cleaner signup form (adds cities, hourly rate, individual/company type) | Public | Submit form | Auto signs in → `/dashboard/cleaner` |
| `/login` | Credentials login form | Public | Sign in | Role-based redirect (see §9) |
| `/forgot-password` | Request a reset link | Public | Submit email | Check email |
| `/reset-password` | Set a new password from a token | Public (needs `?token=`) | Submit new password | `/login` |
| `/verify-email` | Confirms an email-verification token | Public (needs `?token=`) | — (automatic on load) | Dashboard banner clears |
| `/cleaners` | Directory — filterable list of active cleaners | Public | Filter, open a profile | `/cleaners/[slug]` |
| `/cleaners/[slug]` | Public cleaner profile — bio, rate, reviews | Public (Message/Book require login) | Message, book, view reviews | Login (if anon) → chat modal |
| `/cleaners/book-team` (added 2026-08-19) | Multi-cleaner booking form — reached via "Book multiple cleaners" select-mode on the directory (`?slugs=a,b,c`) | `CUSTOMER` only (redirects through `/login?return=` otherwise) | Pick a shared tier, date/time, address; submit one request to all selected cleaners | `POST /api/bookings/multi` → `/dashboard` |
| `/dashboard` | **Home, restructured 2026-08-21** — banners/notifications (verify email, awaiting-confirmation count, reviews-needed), welcome heading, upcoming-only confirmed bookings (no history) | Authenticated (redirects `CLEANER` → `/dashboard/cleaner`) | View/cancel an upcoming job, chat isn't here — see `/dashboard/messages` | `/dashboard/bookings`, `/dashboard/profile` |
| `/dashboard/bookings` | **Added 2026-08-21** — full awaiting-confirmation/upcoming/history bookings list, no banners | Authenticated | Cancel booking, file dispute/no-show, leave a review, manage recurring series | — |
| `/dashboard/messages` | **Added 2026-08-21** — thread list + chat, no banners; `?open=<id>` auto-expands a thread (used by "Book again") | Authenticated | Chat | `/cleaners/[slug]` (find-a-cleaner CTA in the empty state → `/dashboard/search`) |
| `/dashboard/cleaner` | **Home, restructured 2026-08-21** — banners/notifications (verify email, disputes, new booking requests, payout, profile completion, ID verification), welcome heading, badge-gated referral card, upcoming-only confirmed bookings (no history) | `CLEANER` only | Confirm/complete an upcoming job, chat isn't here — see `/dashboard/cleaner/messages` | `/dashboard/cleaner/bookings`, `/dashboard/cleaner/edit` |
| `/dashboard/cleaner/bookings` | **Added 2026-08-21** — full needs-response/upcoming/history bookings list, no banners | `CLEANER` only | Confirm/decline/complete jobs, contest/corroborate no-shows, manage recurring series | — |
| `/dashboard/cleaner/messages` | **Added 2026-08-21** — thread list + chat, no banners | `CLEANER` only | Chat | — |
| `/dashboard/cleaner/edit` | Edit cleaner profile (bio, photos, rate, cities, languages, availability, service tiers/add-ons — added 2026-08-19) | `CLEANER` only | Save profile, upload photos | `/dashboard/cleaner` |
| `/dashboard/cleaner/disputes` | List + detail of disputes filed against this cleaner | `CLEANER` only | Submit a one-time response | — |
| `/dashboard/cleaner/earnings` | Payout setup + held/blocked/paid balance, per-job payout status | `CLEANER` only | Start Stripe Connect onboarding | Stripe-hosted onboarding (external), back here on return |
| `/dashboard/profile` | Shared account settings (any role) | Authenticated | Sign out; customers manage saved addresses; cleaners get links to edit their profile, Earnings (added 2026-08-21), and their public profile | — |
| `/dashboard/search` | App-shell copy of the public cleaner directory (added 2026-08-19) — same `CleanersDirectoryView` component, reached via `BottomTabBar`'s Search tab | Authenticated | Filter/sort, favorite, start a team-booking selection | `/dashboard/cleaners/[slug]`, `/cleaners/book-team` |
| `/dashboard/cleaners/[slug]` | App-native counterpart to `/cleaners/[slug]` (added 2026-08-21) — same `CleanerProfileView` component, reached whenever a logged-in visitor clicks a cleaner card, so browsing a profile never drops back into marketing chrome. `CleanerCard`, the breadcrumb "Find a cleaner" link, and the desktop Navbar's "Find a cleaner" link are all session-aware and route here instead of `/cleaners/[slug]` once logged in | Authenticated (any role) | Message, book, view reviews | Login redirect n/a here (route itself requires auth) |
| `/admin/analytics` | Business-level metrics — bookings/revenue/dispute trends | `ADMIN` only | Read-only | — |
| `/admin` | Verification queue | `ADMIN` only | Approve/reject cleaner ID submissions | — |
| `/admin/disputes` | Dispute resolution queue | `ADMIN` only | Rule for customer or cleaner | — |
| `/admin/cancellations` | Cancellation ledger | `ADMIN` only | Read-only | — |
| `/admin/payouts` | Failed cleaner payouts — single- and multi-cleaner (added 2026-08-19) | `ADMIN` only | Manually retry a `FAILED` transfer | — |
| `/admin/team-bookings` | No-show report queue for multi-cleaner bookings (added 2026-08-19, rebuilt 2026-08-20 — see §11) | `ADMIN` only | Review claim/contest/corroborations, confirm or reject, choose how the forfeited share is reallocated | — |

**Not pages, but worth listing alongside them** — three Vercel Cron jobs (all bearer-auth'd via `CRON_SECRET`, no UI, `vercel.json`):
- `GET /api/cron/cleanup-chat-photos` — deletes chat photos older than 90 days and blanks the message's `photo_path`.
- `GET /api/cron/auto-resolve-disputes` — closes any dispute past its 24h SLA with a forced full customer refund; see §7. **Scheduled once daily (03:15) as of 2026-08-19** — was `*/15 * * * *` for a few hours, but Vercel's Hobby plan only allows once-per-day cron schedules and silently fails every deploy with a more frequent expression (this blocked all deploys from ~16:43 to ~23:xx on 2026-08-18, caught only because deployments visibly stopped). The *actual* enforcement doesn't depend on cron cadence either way — `GET /api/admin/disputes` runs the same check lazily on every load, so the cron is a once-a-day backstop for the case nobody's opened that page, not the primary mechanism.
- `GET /api/cron/release-payouts` — transfers any cleaner payout whose hold has cleared; see §4. **Scheduled once daily (03:30) as of 2026-08-19**, same reason and same lazy backstop pattern (`GET /api/cleaner-profiles/me/earnings`, `GET /api/admin/users`) as above.
- `GET /api/cron/recompute-response-times` — added 2026-08-20, recomputes every active cleaner's "typically responds in..." stat (see §2's Cleaner flow). Scheduled once daily (03:45), staggered after the three above. No lazy backstop, unlike the others — see that section for why.
- `GET /api/cron/charge-recurring` — added 2026-08-21, spawns and off-session-charges the next due occurrence for every active recurring booking series; see §11's "Recurring bookings". Scheduled once daily (04:00), the next free slot in the stagger. No lazy backstop — same reasoning as the response-times cron, there's no single natural page load to backstop from since this scans every series.

Also not a page, not a cron — a webhook **receiver**, added 2026-08-19 directly as a follow-up to the deploy outage above: `POST /api/webhooks/vercel-deploy` verifies Vercel's `x-vercel-signature` (HMAC-SHA1 over the raw body, keyed by `VERCEL_WEBHOOK_SECRET`) and emails admin on a `deployment.error` event, same `sendAdminAlertEmail` path as everything else in §6. Requires a one-time manual step outside this codebase — registering the endpoint in the Vercel dashboard (Project Settings → Webhooks) and copying the generated secret into `VERCEL_WEBHOOK_SECRET` — the code side is done but inert until that's done, same pattern as the Stripe webhook's own setup step.

**Confirmed rough edge:** what an `ADMIN` sees at `/dashboard` (as opposed to `/admin`) isn't handled explicitly — the redirect in `(app)/layout.tsx` only special-cases `CLEANER`. Verified against a live admin session (2026-08-18): an admin visiting `/dashboard` renders "Welcome back, {name}" with the booking/message panels stuck on their loading skeleton forever — `dashboard/page.tsx`'s data-fetch effects are gated on `role === 'CUSTOMER'`, so they never fire for `ADMIN`. Not a crash, just a dead end; no redirect exists to bounce an admin back to `/admin`.

---

## 2. End-to-end user flows by role

### Customer

**Sign up & onboarding**
1. `/register` → `POST /api/auth/register` (`api/auth/register/route.ts:20-117`). Zod-validated, rejects duplicate email (409), bcrypt cost 12, inserts `users` row with `role: 'CUSTOMER'`.
2. Client immediately calls `signIn('credentials', …)` (`register/page.tsx:45`) — **the account is usable before the email is verified.** Verification only ever gates a dismissible dashboard banner, never access.
3. A verification email sends non-blocking (errors swallowed) with a 24h token.
4. Clicking the link hits `/verify-email?token=…` → `POST /api/auth/verify-email`, flips `email_verified = true`.
5. "Resend" on the dashboard banner is rate-limited to once per 5 minutes.
6. Forgot/reset password: always responds success regardless of whether the email exists (anti-enumeration); reset token valid 1h, one live token per user.

**Discover & message a cleaner**
1. `/cleaners` fetches `GET /api/cleaners` (public, no auth) — every `ACTIVE` cleaner profile, sorted by rating then review count. All filtering (city, rate, rating, gender, languages, availability, cleaner type, verified-only) happens **client-side** over the full result set — there's no server-side filter endpoint.
2. `/cleaners/[slug]` fetches the public profile plus a server-resolved `is_own_profile` flag (so a cleaner viewing themself sees a disabled CTA without their `user_id` ever reaching the client). Reviews load separately.
3. Clicking Message/Book: logged-out → redirected to `/login?return=/cleaners/{slug}`, which `login/page.tsx` reads via `safeReturnPath()` and honors after a successful CUSTOMER login (restricted to same-origin relative paths — CLEANER/ADMIN logins always land on their own dashboard regardless of `return`). Logged-in as `CLEANER` → toast refusal, cleaners can't message cleaners. Logged-in as `CUSTOMER` → `POST /api/introductions`, an idempotent find-or-create on the unique `(customer_id, cleaner_profile_id)` pair.
4. Chat opens in `ChatModal`/`ChatPanel`. Realtime via a Postgres-changes subscription on `messages`, authenticated per-connection with a custom Supabase-compatible JWT minted at `/api/supabase-token`.
5. The very first message either party sends in a thread triggers a one-time "you have a new conversation" email to the other party; never fires again for that thread.
6. **Favoriting a cleaner** (added 2026-08-18) — a heart toggle on the directory card (next to the rate) and the profile page (next to the name), `CUSTOMER`-only (hidden entirely for `CLEANER`/`ADMIN`, redirects a logged-out click to `/login?return=...`). Deliberately decoupled from `introductions`/bookings — no prior contact required, per the 2026-08-18 decision. `POST /api/favorites` / `DELETE /api/favorites/[cleanerProfileId]`, backed by a new `favorites` table (`customer_id, cleaner_profile_id`, unique pair). `GET /api/cleaners` and `GET /api/cleaners/[slug]` both include a per-row `is_favorited` boolean for the requesting customer (defaults `false` for anyone else). A "My favorites" toggle pill on `/cleaners` (customer-only, next to the results count — not part of `FilterBar`'s own filter state) client-side filters the directory to just favorited cleaners. "Repeat-book same cleaner" from the original ask is satisfied by this same list plus the existing booking flow, not a separate mechanism — favorite a cleaner once, then re-open their card/profile to book again without re-searching.

**Booking a cleaner** — see [§3](#3-the-booking-lifecycle) for the full state machine. In short: request from inside the chat panel (card authorised, not charged) → cleaner has 24h to confirm (charges the card) or decline → job happens → cleaner marks it complete (≥4 photos required) → review prompt appears.

**Cancelling** — either party, from a `REQUESTED` or `CONFIRMED` booking. Full refund if cancelled 24h+ before the scheduled start; no refund inside that window (binary, not prorated).

**Reviewing** — appears inline under a completed booking once, per booking, if no review exists yet. "Skip" is session-local state only — it comes back on reload.

**Disputing a completed job** — `POST /api/disputes`, filed from a "File dispute" action on the completed booking's card in `dashboard/page.tsx`, within a 24-hour window of `completed_at`. One dispute per booking (unique constraint on `booking_id`); confirms to the customer by email with the 24-hour admin SLA (and that an unreviewed case auto-resolves to a full refund — see §7), and alerts admin.

### Cleaner

**Sign up & profile**
1. `/register/cleaner` → same `POST /api/auth/register`, `role: 'CLEANER'` plus `cities[]`, `hourly_rate_eur`, `cleaner_type`. The route immediately creates a **stub `cleaner_profiles` row with `status: 'ACTIVE'`** — the profile is public and bookable from the moment of signup, complete or not.
2. `/dashboard/cleaner/edit` PATCHes `/api/cleaner-profiles/me` against an explicit field allowlist (`display_name, bio, photo_url, cover_photo_url, cities, hourly_rate_eur, cleaner_type, gender, languages, availability`). Profile photo upload is a separate endpoint, fixed path per user (`{user_id}/avatar.jpg`), upserted. **Cover photo was removed from the UI (2026-08-18)** — the card/profile redesign dropped it in favour of a compact, info-first layout; the edit form no longer has an upload control for it and neither card nor profile page render it. `cover_photo_url` is still a live column and the API/allowlist still accept it (harmless, unreachable from the app) — dropping the column itself was left out of scope.
3. The cleaner dashboard nudges profile completion while bio, photo, cities, or weekly availability are missing (availability added to this check 2026-08-18, see below — the nudge is still a banner only, no enforcement blocking the profile from search).
3b. **Weekly availability, mandatory** (added 2026-08-18) — a simple weekly-recurring calendar (Mon–Sun, whole-hour start/end per day, no one-off exceptions in v1) lives in the edit form, folded into the same "complete your profile" flow. Defaults to fully unset (never assumed available), and the edit form's own save validation blocks saving with zero days set (same pattern as the existing cities requirement) — but nothing yet stops an incomplete profile from being bookable; "mandatory" here means the nudge + save-time validation, not a search-visibility gate. Fixed a real bug in the same change: the column previously had two incompatible shapes live at once (edit form wrote a plain string array, every read site expected an object keyed by day-tag) — saved availability silently never displayed or matched the directory filter for any cleaner. See `src/lib/availability.ts` for the shared type/helpers now used by all four read/write sites. Booking-flow enforcement (rejecting a request outside a cleaner's set hours) is not built — flagged as future work, not decided scope for this pass.
4. ID verification submission — **no entry point exists.** See [§6](#6-known-gaps-and-half-built-flows).
5. Once (however it happens) verification is approved, `cleaner_profiles.verified = true` and a blue badge appears on the public profile and directory card.

**Working bookings**
1. New request → email + system-message pill in the thread. 24h to `CONFIRM` (charges the customer's saved card off-session) or `DECLINE` (never charged).
2. Upload completion photos incrementally while `CONFIRMED` (private bucket, signed URLs).
3. `COMPLETE` once the scheduled date has arrived and ≥4 photos exist. Cleaner's stats (`avg_rating`, `review_count`, `unique_customer_count`, `total_jobs_count`) update via DB trigger, never from application code.
4. Disputes: `/dashboard/cleaner/disputes` shows any dispute filed against a completed job, with a red banner counting `OPEN` ones with no response yet. One response allowed, then locked.
5. **Countdown on upcoming jobs, added 2026-08-21** (Todoist "cleaner home view" spec): every not-yet-due confirmed booking card (`CleanerBookingCard`, shared between `/dashboard/cleaner` and `/dashboard/cleaner/bookings` — see §1's "Cleaner dashboard IA refactor") shows a relative countdown ("In 20 hours", "In 4 days") instead of the old "You can mark this complete on {date}" text, which just repeated the date already in the card's own summary line. Computed on render (`jobCountdownLabel()`), no ticking timer — same lightweight approach as the `hoursLeftToRespond()` "time left to respond" label and the admin disputes SLA countdown.
6. **Where each group lives, post-2026-08-21 IA refactor**: "Needs response" (REQUESTED) and "History" (COMPLETED/CANCELLED) only render on `/dashboard/cleaner/bookings`; `/dashboard/cleaner` (Home) shows only the "Upcoming" (CONFIRMED) group, soonest-first, no cap, plus a notification banner linking to Bookings when there are pending requests. `groupBookingsByPriority()` (`src/lib/utils`) is still the shared sort/bucket function underneath both — and the customer dashboard, which got the identical Home/Bookings split the same day (see §1) using its own "awaiting confirmation" wording rather than "needs response" (the booking isn't the customer's to act on).

**Referrals & badges** (added 2026-08-20)
1. Every cleaner has a `cleaner_profiles.referral_code` (short random string, generated at signup by `POST /api/auth/register`; backfilled for every pre-existing profile via `src/scripts/backfill-referral-codes.js`, which only ever needs re-running if a reseed reintroduces rows without one). The dashboard's "Invite a cleaner, earn a badge" card (`/dashboard/cleaner` Home, hidden once `referred_friend` is already earned — see §1's IA refactor) shares it as `{APP_URL}/get-started?ref=<code>`. **Native share sheet, added 2026-08-21** (Todoist): feature-detects `navigator.share` on mount (`canNativeShare` state, set post-mount only to avoid an SSR hydration mismatch — `navigator` doesn't exist server-side) and, where available, opens the OS share sheet (WhatsApp, Messages, Telegram, email, etc. — no installed app required) with a pre-filled bilingual message instead of just copying the link; the button label switches between "Share link" and "Copy link" to match. Falls back to the pre-existing copy-to-clipboard behavior wherever the API isn't available, or if the share call fails for any reason other than the user cancelling (`AbortError`, a deliberate no-op). Not gated on mobile-vs-desktop specifically — pure feature detection, so it also picks up newer desktop Chromium builds that support it.
2. Landing on `/get-started?ref=<code>` stashes the code in `sessionStorage` (`ReferralCapture`, a tiny client component with no visible UI) so it survives the get-started → for-cleaners → register/cleaner hop through separate page loads. `register/cleaner` reads it back (preferring a `?ref=` directly on its own URL if present) and sends it as `referred_by_code` in the registration POST.
3. `POST /api/auth/register` looks up the referring cleaner by code (must be `ACTIVE`; an unknown/absent code is silently ignored, never blocks signup), stamps the new profile's `referred_by_cleaner_profile_id`, and awards the referrer the `referred_friend` badge.
4. **V1 badge system** (`cleaner_badges` table, `src/lib/badges.ts` for the server-only award functions + `src/lib/badgeConstants.ts` for the client-safe types/labels/display-collapsing split — a client component importing the award-function file would otherwise pull `next/headers` into the browser bundle). Insert-only log, never updated/deleted — a milestone badge gets one row per tier crossed, the display layer (`displayBadges()`) picks only the highest tier per category. `tier` is `''` (not `null`) for the three non-tiered badges specifically because Postgres treats every `NULL` as distinct under a unique constraint, which would silently let a non-tiered badge be inserted more than once.
   - `referred_friend` — awarded per (3) above.
   - `completed_profile` — same "complete" definition as the profile-completion nudge (bio, photo, cities, availability all set); checked on every profile PATCH and on every profile GET (dashboard load).
   - `verified_id` — awarded from the admin verification-approve action (`PATCH /api/admin/verifications/[id]`).
   - `cleans_milestone` (tiers: first job, 25, 50, 100, 250) — checked right after a booking's `COMPLETE` transition, for the single cleaner or every multi-cleaner assignee.
   - `tenure_milestone` (tiers: 1 month, 6 months, 1 year) — purely time-based, no triggering event, so it's a lazy check on every profile GET rather than a new cron.
   - Cleaners active before this shipped needed one-time backfills for the three trigger-based badges they'd already have qualified for: `src/scripts/backfill-milestone-badges.js` (cleans + tenure) and `src/scripts/backfill-verified-id-badges.js` (verified). Re-running either is always safe — the unique constraint makes every award idempotent.
5. Badges render as small gold chips (`BadgeRow`) on the public cleaner profile, fetched server-side alongside the profile/reviews (`getCleanerBadgesBySlug` in `src/lib/cleaners.ts`). Cosmetic/trust-signal only, confirmed no effect on search ranking.
6. **Admin referral-chain visibility — added 2026-08-20**: `GET /api/admin/users` now resolves each cleaner's `referred_by_cleaner_profile_id` to the referrer's display name (one batched lookup query, not one per row) and shows a "Referred by {name}" line on `/admin/users`. No admin action tied to it, purely informational — was explicitly deferred as a nice-to-have when the referral link itself shipped, built here as a quick follow-up rather than its own analytics-segmentation feature.

**"Typically responds in..." — added 2026-08-20, deliberately NOT part of the badge system above** (a live descriptive stat, not an earned achievement — Etsy/Airbnb-style host response label).
1. `cleaner_profiles.typical_response_minutes` (nullable) + `response_sample_size`. For each of a cleaner's introduction threads: the customer's first message in that thread, to this cleaner's first message after it (any message counts, including a system-event one — the point is who's turn it was, not the content). Median across every thread that has a response; threads with no reply yet, or where the cleaner spoke first, don't count.
2. **Recomputed once daily** (`GET /api/cron/recompute-response-times`, `vercel.json`) for every `ACTIVE` cleaner, not live on message send — deliberately, to avoid adding latency to chat (the original task's own steer). No lazy on-page-load backstop either, unlike the other crons in this app — there's no single natural page load that's "the viewing cleaner's own", since this recomputes everyone. A day's staleness on a cosmetic label is an acceptable tradeoff. Confirmed safe to add as a 4th daily cron: Vercel lifted the per-project Hobby cron count cap to 100 in Jan 2026 (previously 2/team) — the once-daily frequency limit from yesterday's incident is the only real constraint left, and this is staggered at 03:45 alongside the other three.
3. Below `MIN_RESPONSE_SAMPLE` (3) data points, or when the median is too slow to be a flattering label (over 24h), nothing renders — never a discouraging number, never a claim built on 1-2 lucky/unlucky replies. Buckets: within 1 hour / within 3 hours / within a day.
4. Split across two files for the same client/server-boundary reason as the badge system: `src/lib/responseTime.ts` (server-only recompute, imports the admin Supabase client) vs `src/lib/responseTimeConstants.ts` (client-safe bucketing, used by the profile page directly).
5. Rendered as one more row in the profile page's info list (languages/city/type/availability/transport), not a separate section.

### Admin

Covered in full in [§7](#7-admin-flows).

---

## 3. The booking lifecycle

State machine: `REQUESTED → CONFIRMED → COMPLETED`, or `REQUESTED|CONFIRMED → CANCELLED`. Every transition goes through one endpoint: `PATCH /api/bookings/[id]` (`bookings/[id]/route.ts`).

| From | Action | To | Who | What happens |
|---|---|---|---|---|
| — | Request | `REQUESTED` | Customer | `POST /api/bookings`. Validates every field server-side (bedrooms 0–10, bathrooms 1–10, duration 1–12h, address ≤200 chars, date/time not in the past). `service_type` is derived from the cleaner's first offered service, not a form field. `cleaning_type` (STANDARD/DEEP/MOVE_IN_OUT) and any selected `addon_codes` (CARPET/OVEN) are re-validated against the cleaner's own `cleaner_service_offerings` rows here — 409 if the cleaner hasn't actually opted into the requested tier/add-on, regardless of what the form showed (added 2026-08-19, see [§11](#11-data-model-reference)). Inserts the booking, inserts a matching `payments` row as `PENDING` (card authorised via a Stripe SetupIntent client-side beforehand, **not charged** — `amount_eur` is locked in now at the resolved tier rate plus any add-ons plus the flat `BOOKING_FEE_EUR`, see [§4](#4-the-payment-flow)), inserts a `REQUESTED` system message, emails the cleaner. If the payment-row insert fails, the booking insert is rolled back. |
| `REQUESTED` | *(24h passes, no response)* | `CANCELLED` | System (lazy) | **No cron job.** Enforced only on the next `GET /api/bookings` read via `expireOverdueRequests()` — any `REQUESTED` row past its 24h deadline flips to `CANCELLED` and gets a `CANCELLED` system message. The `PATCH` endpoint independently re-checks the same window and returns 409 on a stale confirm/decline attempt. |
| `REQUESTED` | Confirm | `CONFIRMED` | Cleaner only | **The customer is charged here** — an off-session Stripe PaymentIntent against the saved payment method and `stripe_customer_id`, for the full `amount_eur` (rate + fee), into the platform's own Stripe balance. Success → `payments.status = PAID`. Failure → `payments.status = FAILED`, 402 returned with the Stripe error surfaced to the cleaner, booking stays `REQUESTED`. Deliberate design: charging at confirm-time (not completion) discourages late cancellations and avoids ~7-day card-hold expiry on bookings confirmed well in advance. |
| `REQUESTED` | Decline | `CANCELLED` | Cleaner only | Never charged (payment was still `PENDING`), so no refund logic runs. |
| `REQUESTED` or `CONFIRMED` | Cancel | `CANCELLED` | Either party | `cancellation_reason` and `cancelled_by` stamped. If `payments.status === 'PAID'`: full refund if cancelling ≥24h before the scheduled start, otherwise none. A failed refund call is logged, not retried, and does **not** block the cancellation — flagged in-code as needing manual Stripe-dashboard follow-up. |
| `CONFIRMED` | Complete | `COMPLETED` | Cleaner only | Only once the booking's `date` has arrived **and** ≥4 completion photos are uploaded. `review_prompted_at` and cleaner stats are stamped by the `on_booking_completed` DB trigger, never the route. `payments.payout_release_at` is stamped here too (an informational "held until" for the cleaner — see [§4](#4-the-payment-flow)). Customer gets an email. |

**System messages** — every `PATCH` action inserts a chat-thread pill keyed by the *action* (`CONFIRMED`, `DECLINED`, `CANCELLED`, `COMPLETED`), not just the resulting status — `DECLINE` and `CANCEL` both resolve `status` to `CANCELLED` but read differently in chat.

A Stripe webhook does exist (`POST /api/webhooks/stripe`) for the async state a synchronous API response can't cover — see [§4](#4-the-payment-flow).

---

## 4. The payment flow

| Moment | Stripe action | Payment status | Notes |
|---|---|---|---|
| Booking form opens | `SetupIntent` created (`POST /api/stripe/setup-intent`), confirmed client-side by `BookingPaymentElement` | — | Card is authorised/saved, nothing charged |
| Booking requested | — | `PENDING` | `amount_eur = (tier_rate_eur × duration_hours) + addon_total_eur + BOOKING_FEE_EUR` — the total the customer pays, locked in at the rate quoted now. `tier_rate_eur` is `hourly_rate_eur` for STANDARD or the cleaner's own DEEP/MOVE_IN_OUT rate from `cleaner_service_offerings` (added 2026-08-19); `addon_total_eur` sums any selected flat-priced add-ons. Both are snapshotted per-payment, same reasoning as `platform_fee_eur` (`src/lib/stripe.ts`'s `BOOKING_FEE_EUR`, finalized 2026-08-18 at €0.50, was a €1 placeholder) — a cleaner's later price change never rewrites a past booking's breakdown |
| Cleaner confirms | Off-session `paymentIntents.create({ confirm: true, off_session: true })` against the saved payment method, for the full `amount_eur` | `PAID` on success, `FAILED` on error | **This is the actual charge moment** — not at booking request, not at completion. A plain charge into the *platform's own* Stripe balance — not a Connect destination charge, see the payout section below for why |
| Cleaner declines | — | stays `PENDING` (booking → `CANCELLED`) | Never charged, nothing to refund |
| Either party cancels | `stripe.refunds.create(...)` — **only if** `payments.status === 'PAID'` and cancelling ≥24h before the scheduled start | `REFUNDED` if eligible, otherwise stays `PAID` | No partial/prorated refunds — binary on the 24h line. Refunds the full `amount_eur`, fee included |
| Job completed | — | stays `PAID`; `payout_release_at` stamped | Starts the cleaner payout clock — see below |

**Dispute resolution refund behaviour varies by outcome** (see §6/§7): `CLEANER` triggers no payment action; `UNRESOLVABLE` auto-issues a split refund at the admin-chosen percentage the moment it's resolved; `CUSTOMER` stays a deliberate manual action (an admin clicks "Refund customer" in the queue) **unless** the dispute was auto-resolved on SLA timeout (see below), in which case the full refund is issued automatically, same as `UNRESOLVABLE`.

**Disputes auto-resolve to a full customer refund if unreviewed within 24h** (`disputes.resolve_by`) — see §6. Unlike every other refund path in the app, this one is enforced both by a Vercel Cron (`/api/cron/auto-resolve-disputes`, `src/lib/disputes.ts`) and lazily at the top of `GET /api/admin/disputes`, since a plan-capped cron frequency alone could leave a breach sitting well past its 24h SLA.

### Cleaner payouts (Stripe Connect)

The cleaner is never charged a commission — they keep their full rate; the platform's only revenue is the flat `BOOKING_FEE_EUR`. This is a Connect **"separate charges and transfers"** setup, not a destination charge: the charge above lands entirely in the platform's balance, and the cleaner's cut only moves via a distinct `stripe.transfers.create()` call once their payout is actually due — see `src/lib/payouts.ts`. A destination charge's `application_fee_amount`/`transfer_data.destination` would instead transfer to the cleaner the moment the charge succeeds (at CONFIRM, days before the job happens), which can't be reconciled with holding the payout through the post-completion dispute window.

- **Onboarding** — `POST /api/cleaner-profiles/connect/onboard` creates a Stripe Connect **Express** account for the cleaner (`stripe.accounts.create`, `country: 'CY'`, only the `transfers` capability requested — the connected account never processes a card charge itself, so onboarding only needs identity + bank details, not full payment-processing KYC), then returns a Stripe-hosted Account Link. Any SEPA IBAN works, not specifically a Cypriot bank. This is entirely separate from the `id_photo_url`/`selfie_photo_url` trust-and-safety verification (§2/§7) — a cleaner can have one without the other; they serve different purposes (public trust badge vs. Stripe's own financial KYC gating payouts).
- **Tracking onboarding status** — `cleaner_profiles.stripe_connect_details_submitted`/`stripe_connect_payouts_enabled` mirror Stripe's own `Account.details_submitted`/`payouts_enabled`, kept current by the `account.updated` case in the webhook (`POST /api/webhooks/stripe`) — fires automatically for any Express account this platform created, no separate Connect webhook subscription needed.
- **Payout hold** — a completed booking's payout sits `PENDING` until 24h after `completed_at` with no dispute filed, or until a filed dispute resolves (whichever applies) — deliberately mirrors the 24h dispute filing/resolution SLA (§6) so the two clocks move together: max ~48h from completion to payout-eligible, not a week.
- **Amount** — full rate if no dispute; reduced by the dispute's `refund_percentage` if one was ruled (a 100% customer-favor ruling means the cleaner gets nothing for that job; a 50% split means half). Computed once, at release time, never re-computed after — see `evaluateReadiness()`/`processOneCandidate()` in `src/lib/payouts.ts`.
- **Release** — `releaseDuePayouts()` (`src/lib/payouts.ts`) scans every payment still owed. If the cleaner's Connect account isn't `payouts_enabled` yet, the payout sits `BLOCKED` (amount known, nothing sent) until they finish onboarding — `releaseBlockedPayoutsForCleaner()` is called directly from the `account.updated` webhook case so a backlog releases the moment onboarding completes, not on the next cron tick. Enforced two ways, same reasoning as the dispute auto-resolve job: a Vercel Cron (`/api/cron/release-payouts`, once daily — see §5's cron list for why it's not more frequent) and a lazy check at the top of `GET /api/cleaner-profiles/me/earnings` and `GET /api/admin/users`.
- **Failure** — a failed transfer sets `payout_status = 'FAILED'` (doesn't unwind anything else) and alerts admin by email. `releaseDuePayouts()` never re-scans `FAILED` rows on its own (only `PENDING`/`BLOCKED`), so nothing retries automatically. **Admin recovery, added 2026-08-19**: `/admin/payouts` (nav tab "Failed payouts") lists every `FAILED` payout across both single-cleaner (`payments`) and multi-cleaner (`booking_assignments`) bookings, each with a manual retry button — `POST /api/admin/payments/[id]/retry-payout` / `POST /api/admin/booking-assignments/[id]/retry-payout`, both thin wrappers around `retryFailedPayout()`/`retryFailedAssignmentPayout()` in `src/lib/payouts.ts`.
- **EUR/GBP currency mismatch — root cause fixed 2026-08-20**: the platform's Stripe account only ever had GBP as a settlement currency, but payouts are created with `currency: 'eur'` — every EUR transfer failed with `balance_insufficient` since there was no EUR balance to draw from (4 real completed bookings got stuck `FAILED` this way). Fixed by adding EUR as a second settlement currency on the live Stripe account (Balances → "+ Add settlement currency" — a live-mode-only account setting, greyed out in test mode) and setting it as default, so EUR charges now accrue their own balance instead of auto-converting into the GBP one.
- **A second, independent bug in the retry mechanism itself — also fixed 2026-08-20**: the original retry code reused the *exact same* idempotency key as the failed original attempt (`payout-${bookingId}`). Stripe caches an idempotent request's response for 24h, so retrying with an unchanged key just replayed the *original* `balance_insufficient` error verbatim — confirmed live via the Stripe Dashboard's API request log, which showed each retry pointing back at the very first failed request as its "Original request." This meant no retry could ever succeed, even after the currency fix landed, until 24h after the first retry attempt. Fixed by giving each manual retry its own key (`payout-retry-${bookingId}-${Date.now()}`); the existing `payout_status !== 'FAILED'` guard is what actually prevents a double-transfer once a retry succeeds. All 4 stuck payouts confirmed `PAID` with real Stripe transfer IDs after both fixes landed.
- **Cleaner-facing view** — `/dashboard/cleaner/earnings`: a "set up payouts" prompt if not onboarded (or a "Stripe is reviewing" note if submitted but not yet approved), a held/blocked/paid balance summary, and a per-job list. Reachable as a third desktop tab alongside Bookings/Messages on `/dashboard/cleaner` (`DashboardTabs` supports an `href` tab that navigates instead of switching an in-page panel — added 2026-08-18), and a dashboard banner still links here whenever setup is incomplete or a balance is owed. Mobile has no dedicated nav slot yet (the bottom tab bar is a fixed 5-icon set shared with the customer role) — reachable there only via the banner.
- **Customer-facing breakdown, after booking** — the booking detail modal (dashboard + chat) shows cleaner's rate + booking fee = total whenever a payment is present, reading `payments.amount_eur`/`platform_fee_eur` via `GET /api/bookings`'s `payments` embed.
- **Customer-facing breakdown, before booking** (added 2026-08-18; tier/add-on aware as of 2026-08-19) — `BookingFormModal` shows the same rate/fee/total layout live, before submit, updating as the customer changes duration or picks a tier/add-on. Display-only estimate, not trusted: the real charge is computed server-side once, at `POST /api/bookings` (booking-**REQUEST** time, not `CONFIRM` — a stale claim previously here said CONFIRM; `CONFIRM` charges whatever `amount_eur` was already locked in at REQUEST, unchanged, see [§3](#3-the-booking-lifecycle)). The rate + flat fee + the cleaner's opt-in tiers/add-ons reach the client via `hourly_rate_eur`/`booking_fee_eur`/`cleaner_service_offerings` now included in `GET /api/introductions` (customer branch) and `GET /api/cleaners/[slug]`, threaded through `ChatPanel`/`ChatModal` — `BOOKING_FEE_EUR` itself stays server-only, never imported client-side.
- **Service tiers + add-ons** (added 2026-08-19) — `cleaner_service_offerings` (one row per cleaner per opt-in `code`, absence = not offered) lets each cleaner enable DEEP/MOVE_IN_OUT tiers and CARPET/OVEN add-ons at their own price, configured on `/dashboard/cleaner/edit` (`PATCH /api/cleaner-profiles/me/offerings`, whole-set replace). STANDARD is the always-on baseline (`cleaner_profiles.hourly_rate_eur`) and never gets a row here. The booking form filters to only the specific cleaner's enabled offerings; `POST /api/bookings` independently re-validates against the same table as the actual authority (see the booking-lifecycle table above). Directory/profile headline pricing (`CleanerCard`, `/cleaners/[slug]`) deliberately still shows only the base STANDARD rate — not redesigned as part of this change.

---

## 5. The email map

All templates in `src/lib/email.ts`, sent via Resend. As of 2026-08-18 every customer/cleaner-facing template is both locale-aware (branches its full body on the recipient's actual `users.locale`, not just the subject) and personalized (greets the recipient by name) — this was a real, fixed gap: `sendVerificationEmail`/`sendPasswordResetEmail` used to pick a locale-correct *subject* but hardcode an English *body* regardless, and registration/resend-verification/forgot-password all hardcoded `locale: 'en'` on the call itself rather than reading the user's stored preference. Registration now accepts and stores the customer's/cleaner's current locale from whichever locale-prefixed page they registered on.

| Trigger | Function | Recipient |
|---|---|---|
| Registration | `sendVerificationEmail` | new user |
| "Resend" click | `sendVerificationEmail` | self |
| Forgot password | `sendPasswordResetEmail` | requester |
| First message in a thread (once, ever) | `sendNewMessageEmail` | other party |
| Booking requested | `sendNewBookingRequestEmail` | cleaner |
| Booking confirmed (charge succeeded) | `sendBookingConfirmedEmail` | customer — states the amount charged |
| Booking completed | `sendBookingCompletedEmail` | customer — states the 24h dispute-filing window |
| Booking declined | `sendBookingDeclinedEmail` | customer |
| Booking cancelled (either party) | `sendBookingCancelledEmail` | whichever party didn't act |
| ID verification approved | `sendVerificationApprovedEmail` | cleaner |
| ID verification rejected | `sendVerificationRejectedEmail` | cleaner |
| Dispute filed (confirmation) | `sendDisputeFiledConfirmationEmail` | customer — names which booking, by date |
| Dispute resolved | `sendDisputeResolvedEmail` | both parties, separately — framed as WON/LOST/UNRESOLVABLE from each side; names the booking and, for a customer win, the refund amount |
| No-show flagged (added 2026-08-20) | `sendNoShowFlaggedConfirmationEmail` / `sendNoShowContestPromptEmail` / `sendNoShowCorroborationRequestEmail` | filing customer / flagged cleaner / every *other* assigned cleaner on the job, separately |
| No-show resolved (added 2026-08-20) | `sendNoShowResolvedEmail` | customer, flagged cleaner, and (only if `REDIRECT_TO_CLEANER`/`SPLIT` paid one out) the covering cleaner — each framed for their own role |
| Account deleted | `sendAccountDeletedEmail` | the (former) account holder |
| Contact form received | `sendContactSubmissionConfirmationEmail` | submitter — locale comes from the form's own current page locale, since a logged-out submitter has no stored preference |

**Still nothing emails on:** a cleaner responding to a dispute (the admin only sees it on their next queue read, §7/§8) — the "no email on cancellation" and "no email on decline" claims previously here were themselves stale; both already existed.

**Admin-only alerts** (`sendAdminAlertEmail` and everything built on it — refund-failed, payout-failed, new-dispute-filed, **new-no-show-flagged** and a redirect-transfer-failed variant (both added 2026-08-20), **recurring-charge-blocked** (no Stripe customer on file) and **recurring-charge-failed** (added 2026-08-21, see §11's "Recurring bookings"), new-contact-submission, new-support-message, booking-confirmed-admin-copy, **deploy-failed** (added 2026-08-19, fired from `POST /api/webhooks/vercel-deploy` on Vercel's `deployment.error` event — see §5's cron/webhook list)) are internal-only and deliberately English-only, no locale branching — a different, intentional design choice, not a gap.

**Every send is best-effort** — wrapped in try/catch at the call site so a Resend outage never fails the booking, verification decision, or any other action it's attached to. There's also a dev/staging escape hatch: if `RESEND_TEST_EMAIL` is set, every email is redirected there regardless of the real recipient, because Resend's sandbox sender silently drops mail to anyone but the account owner until a custom domain is verified.

---

## 6. Known gaps and half-built flows

Ranked by how much is already built behind each gap.

1. **Cleaner ID-verification submission has no entry point.** `cleaner_profiles.id_photo_url`, `selfie_photo_url`, `id_submitted_at` are fully consumed by the admin queue — including the reject-clears-`id_submitted_at`-for-resubmission logic — but nothing in `PATCH /api/cleaner-profiles/me`'s field allowlist, and no dedicated upload route/UI, lets a cleaner set them. A cleaner cannot get themself into the verification queue through the app today.
2. ~~`schema.sql` undersells the live schema.~~ **Resolved 2026-08-19** — `verification_tokens` and the four `cleaner_profiles` columns this used to flag as missing (`cities`, `cleaner_type`, `gender`, `is_mock`) are all present in the checked-in schema file now. See §11's "`schema.sql` drift — resolved" for detail.
3. **A skipped review prompt doesn't persist.** `skippedReviewIds` is component state on the customer dashboard — reload the page and a skipped prompt reappears.

~~4. Multi-cleaner bookings work end-to-end except no-show marking.~~ **Resolved 2026-08-19, revised 2026-08-20** — the admin-only v1 was rebuilt into the full customer-flag → corroborate → contest → admin-resolve workflow. **Not yet DB-verified** (schema migration pending, see §11). See §11's "Multi-cleaner bookings" section for the full breakdown.

*(2026-08-18: several stale claims previously listed here or in §4/§10 were confirmed fixed and removed rather than left inaccurate — "filing a dispute has no entry point," "`cleaner_status` `PAUSED`/`SUSPENDED` are unused," "no Stripe webhook," "no idempotency key on confirm" and "a failed refund doesn't alert anyone" all describe an earlier state of the codebase, not the current one. See the "Regenerate FLOWS.md against current code" Todoist task — this doc has drifted in more places than just these.)*

*(2026-08-18: the `chat_notifications` gap previously listed here is now resolved by removal, not by wiring it up — see [§3](#3-the-booking-lifecycle)/messaging notes: it was confirmed to have zero reads or writes anywhere in app code, no RLS policy (so it wasn't even reachable under RLS), and no trigger populating it. Unread state was already fully handled by the live per-request computation in `attachLastMessages()`, and the one-time "new conversation" email alert already runs off `introductions.last_emailed_at` / `support_threads.last_emailed_at` — neither depends on this table. Dropped from `schema.sql`; still needs the corresponding `DROP TABLE chat_notifications;` run manually in the Supabase SQL Editor against the live DB, same as any other schema.sql change.)*

---

## 7. Admin flows

Six tabs under `/admin/**`, all gated to `token.role === 'ADMIN'` by middleware. Nav: `src/components/admin/AdminNav.tsx`.

### Analytics (`/admin/analytics`)
- Added 2026-08-18. Business-visibility, not moderation — deliberately separate from the operational tabs below (verifications/disputes/cancellations/users are all row-listing queues for individual cases; this is the only aggregate/trend view in the admin panel).
- `GET /api/admin/analytics` — a single read-only response: a bookings funnel, lifetime totals (registered customers, active cleaners, platform revenue), two rate stats (dispute rate + auto-resolve-on-timeout rate), and a 12-week Monday-anchored weekly series (bookings count, revenue) computed in application code from raw `bookings`/`payments` rows — no Postgres aggregation function, no date-range query helper existed anywhere in the codebase before this, so it was written from scratch.
- **Bookings funnel, replacing the old single "Total bookings" number — REDESIGN 2026-08-21** (Todoist, following the AUDIT): three stages — Requested (every booking ever created, any status; what "Total bookings" used to mean alone), Accepted (currently `CONFIRMED` or `COMPLETED`), Completed (unchanged) — plus a drop-off % shown under Accepted and Completed (`% of requested`, `% of accepted`). **Accepted is an approximation**: there's no `confirmed_at` timestamp or status-history table, only current status, so a booking confirmed and *later* cancelled is indistinguishable from one cancelled before ever being confirmed — both simply aren't `CONFIRMED`/`COMPLETED` today and fall out of the count the same way. A `confirmed_at` column (mirroring `completed_at`) would close this gap; flagged, not built.
- **"Active cleaner" redefined — REDESIGN 2026-08-21**: was `cleaner_profiles.status = 'ACTIVE'` alone, a pure snapshot with no recency signal. Now also requires a login within the last 30 days (`ACTIVE_CLEANER_WINDOW_DAYS` in the route), backed by a new `users.last_login_at` column stamped best-effort (non-blocking, errors swallowed, matching the existing verification-email pattern) in NextAuth's `authorize()` — the first time any login-recency data has existed in this schema. Both conditions apply: a recently-logged-in but `PAUSED`/`SUSPENDED` cleaner still doesn't count.
- **Booking-frequency cohorts, replacing the old "repeat-customer rate %" — REDESIGN 2026-08-21**: the single blended rate excluded customers with zero completed bookings from *both* sides of its own fraction. Now the full distribution shows on the customer-segments panel: no completed bookings / one-time (1) / occasional (2–4) / regular (5+), covering every registered customer. Basis is still completed bookings only, same as the old rate, so it can't disagree with itself.
- **Revenue** here means the platform's own cut (`platform_fee_eur`, not gross booking value), recognized at charge time. **Fixed 2026-08-20**: no longer gated on `status === 'PAID'` alone — that excluded a payment's *entire* fee the moment *any* refund landed, even a partial one (an UNRESOLVABLE dispute split, a no-show redirect/split against just one cleaner's share of a multi-cleaner booking). Now nets the fee against `payments.refunded_amount_eur` (a running total of everything actually refunded, added this same pass) as a fraction of `amount_eur`, and counts `PAID`/`REFUNDED`/`REFUND_FAILED` payments alike (a failed refund attempt never actually moved the money, so it still counts in full) — only genuinely-uncharged `PENDING`/`FAILED` payments contribute nothing. Applies to both the lifetime total and the per-week chart, which had the identical bug. Found during the admin analytics audit (Todoist), fixed the same session.
- **Dispute rate** is disputes ÷ completed bookings (a dispute can only be filed on a `COMPLETED` booking, so this is the right denominator, not total bookings).
- Charts are a small hand-rolled CSS bar chart component in the page itself, not a charting library — no such dependency existed in the repo and the data shape (a handful of weekly buckets, two series) didn't justify adding one.
- **Weekly-chart date range, added 2026-08-21** (Todoist): was hardcoded to 12 weeks with no control. A single segmented control (4 weeks / 12 weeks / 6 months / all time — presets only, no raw date picker, per the task's own steer) sits above both charts and drives both identically via `?range=` on `GET /api/admin/analytics`. Everything else in the response (totals, funnel, segments) is unaffected by `range` — the whole payload is still one request, just re-fetched on range change; the frontend keeps showing the previous range's data while the new one loads rather than blanking the page. "All time" computes its week count from the earliest booking/payment already in the fetched rows (no extra query) rather than a fixed number.
- **Segmentation — added 2026-08-20**: below the lifetime totals and weekly charts, two more sections — customer segments (by booking frequency: one-time/occasional/regular, bucketed on completed-booking count; by dispute outcome, see below) and cleaner segments (by rating; by completed jobs, bucketed to match the cleaner badge milestones so the two views can't disagree; by dispute rate, active cleaners with ≥1 completed job only). All computed in the same `GET /api/admin/analytics` response from data already fetched for the totals above — no new tables, no new queries beyond widening the existing `disputes`/`cleaner_profiles` selects. Cleaner dispute-rate attribution is single-cleaner-booking only — a multi-cleaner whole-job dispute (`disputes.cleaner_profile_id` null) isn't charged to one specific assignee, since that would need a `dispute_assignment_outcomes` join this pass didn't add. Rendered with the same hand-rolled-CSS approach as the weekly charts (a new `SegmentBreakdown` component: label + proportional bar + count), not a new dependency.
- **"By dispute history" rebuilt as "by dispute outcome", 2026-08-21** (audit finding, Todoist): the original three buckets (0/1/2+ disputes filed) counted filings only — a customer who filed and lost every dispute looked identical to one who filed and won every one. `disputes.resolution` (`CUSTOMER`/`CLEANER`/`UNRESOLVABLE`) and `disputes.refund_percentage` (0–100, set at resolution time) already existed as structured outcome data, so this was a query rewrite, not a schema change. New buckets, same "every customer who's ever booked" universe as before: **no disputes filed** · **filed, never won** (every dispute they filed resolved at 0% refund, or is still `OPEN`) · **won a refund** (≥1 dispute resolved with `refund_percentage > 0` — a full `CUSTOMER` ruling or a partial `UNRESOLVABLE` split both count, since both actually moved money their way).

### Verification queue (`/admin`)
- List: `GET /api/admin/verifications` — cleaner profiles where `id_submitted_at is not null and verified = false`, oldest first.
- Detail: a full-screen modal per row — bio, submitted ID photo, selfie, an admin-note textarea.
- Decision: `PATCH /api/admin/verifications/[id]` with `{action: 'APPROVE'|'REJECT', note?}`.
  - **Approve** → `verified = true`, note stored as `verification_note`.
  - **Reject** → `verified` stays `false`, and **clears `id_submitted_at` back to null** so the cleaner could resubmit — this is the only place that field is ever reset, which matters given gap #1 above means there's no resubmission UI either.
  - Either outcome emails the cleaner.

### Dispute resolution (`/admin/disputes`)
- `GET /api/admin/disputes` joins customer, cleaner profile, and booking (including signed completion-photo URLs) — and, before returning, lazily auto-resolves any `OPEN` dispute past its `resolve_by` SLA (see below), so a breach never sits stale just because the cron hasn't ticked yet.
- `PATCH /api/admin/disputes/[id]` with `{resolution: 'CUSTOMER'|'CLEANER'|'UNRESOLVABLE', note?, refund_percentage?}` → stamps `status = 'RESOLVED'`, `resolved_at`, `admin_note`. Both resolved and still-open disputes remain visible in the list.
  - `CLEANER` → no refund.
  - `CUSTOMER` → no automatic refund; a "Refund customer" button appears in the detail view for the admin to trigger manually.
  - `UNRESOLVABLE` (a neutral split decision) → auto-issues a Stripe refund immediately at the admin-chosen percentage (default 50%). On success, `payments.status → REFUNDED` and `refunded_at` is stamped, same as a cancellation refund (fixed 2026-08-18 — this previously left `payments.status` at `PAID` on a successful split refund, so the money moved but the row didn't reflect it). A failed refund doesn't unwind the ruling — `payments.status → REFUND_FAILED` and an admin alert email fires instead.
- Emails both parties, same note, framed as won/lost from each one's own side (`UNRESOLVABLE` gets its own neutral framing).
- **Filing window & resolution SLA: 24h/24h** (`src/app/api/disputes/route.ts`'s `FILING_WINDOW_MS`/`RESOLUTION_SLA_MS`) — a customer has 24h from `booking.completed_at` to file, and `disputes.resolve_by` (stamped `created_at + 24h` on filing) is the admin SLA shown as a countdown/overdue badge in the queue.
- **Auto-resolve on SLA timeout** (`src/lib/disputes.ts`'s `autoResolveOverdueDisputes()`, triggered by `/api/cron/auto-resolve-disputes` — Vercel Cron, `vercel.json` — and lazily by the `GET` above): any `OPEN` dispute past `resolve_by` is force-resolved as `CUSTOMER`/100% refund, `disputes.auto_resolved = true`, and the Stripe refund is issued automatically (full refund, same call shape as a cancellation refund — sets `payments.status → REFUNDED` on success, `REFUND_FAILED` + admin alert on failure). Both parties get the standard resolved email, but with a note explaining it was a timeout default, not a considered ruling. The queue badges these distinctly ("Auto-resolved — SLA timeout") from an admin's own `CUSTOMER` ruling.
- **Per-customer dispute/refund history** — `/admin/users` shows, inline on each customer's card, "N disputes filed — X auto-resolved, Y admin-resolved" whenever a customer has ≥1 dispute (`GET /api/admin/users` aggregates the `disputes` table by `customer_id`). This is the safety net for the auto-refund-on-timeout policy: a customer repeatedly filing and waiting out the clock for a free refund is visible to admin without a separate query, though nothing currently acts on the pattern automatically (flagging/restricting a repeat customer is still a manual admin judgment call).

### Cancellations (`/admin/cancellations`)
- `GET /api/admin/cancellations` lists every `CANCELLED` booking that has a `cancellation_reason`, joined with who cancelled it and the payment's refund state.
- Purely a visibility tool — refund eligibility was already decided automatically at cancel-time (§3); there's no action to take here.
- Badge states: not-charged / refunded / charged-not-refunded / charge-failed.

### Users (`/admin/users`)
- `GET /api/admin/users` — every registered account, searchable by name/email. Cleaners get Pause/Suspend/Reactivate actions (`PATCH /api/admin/users/[id]`, sets `cleaner_profiles.status`); customers have no actions here.
- Doc note: this tab already existed before Messages was added below — "no user-management screen" was a stale claim, corrected here.
- Also runs the dispute auto-resolve and payout-release lazy checks (same as the queues that own each) before returning, and inlines two per-user aggregates read nowhere else in the UI: a customer's dispute/refund history (§6/§7, "N disputes filed — X auto-resolved, Y admin-resolved") and a cleaner's failed-payout count (§4) — both exist so a pattern needing attention is visible without a separate query.

### Messages (`/admin/messages`)
- One inbox merging two different underlying things, sorted by most recent activity: `support_threads` (a customer/cleaner talking to admin directly, live chat) and `contact_submissions` (the logged-out-friendly contact form, async, no ongoing thread). `GET /api/support/threads` (as ADMIN, returns every thread) + `GET /api/admin/contact`, merged and sorted client-side — not one combined API.
- Support chat reuses the `introductions`/`messages` machinery rather than a second parallel system: `messages.support_thread_id` is a nullable FK sibling to `messages.introduction_id` (exactly one of the two is ever set, enforced by a check constraint), same Realtime-via-Postgres-changes pattern, same `/api/messages` GET/POST endpoints branching on which id is provided. `SupportChatPanel` is a trimmed sibling of `ChatPanel` — no booking-request UI, since a support thread has no booking to nudge.
- A user starts a thread via "Contact support" on `/dashboard/profile` (`POST /api/support/threads`, find-or-create against their own most recent `OPEN` thread — not admin-initiated). Admin replies from the inbox; either side's messages are visible to the thread's owner and to any admin (RLS: `support_threads_own_or_admin`, `messages_own_thread` extended to cover the support-thread branch).
- Notifications: the thread owner's first message triggers a one-time email alert to `ADMIN_EMAIL` (`sendSupportMessageAlertEmail`, same `last_emailed_at`-gated pattern as introductions) — not per-message, and not the reverse: an admin's reply doesn't email the user back (relies on Realtime while they're in the app; not built as a v1 tradeoff, not because it's undesirable).
- Admin can toggle a thread `OPEN ⇄ CLOSED` (`PATCH /api/support/threads/[id]`) and a contact submission resolved/unresolved (`PATCH /api/admin/contact/[id]`) — both toggle rather than one-way, so a mistaken close/resolve has a way back.
- Not built: the admin thread *list* doesn't live-update on a brand-new incoming thread — only messages inside an already-open thread are Realtime. A new thread needs a page reload to appear (or the email alert prompts admin to go look).

### What the schema suggests but the admin panel doesn't have
- ~~No admin view onto `payments` directly~~ **Partially resolved 2026-08-19** — `FAILED` payouts (both `payments` and `booking_assignments`) now have an admin list + manual retry at `/admin/payouts` (see §4's payout Failure note). A stuck `PENDING` payment (never reached `FAILED`, just sitting unswept) still has no dedicated admin list — the lazy checks and daily cron are the only things that move it, no way to see or force one manually.

---

## 8. Cross-party interactions

Every point where one role's action visibly changes what another role sees or can do next.

| Trigger (actor) | What the other party sees | What the other party can now do |
|---|---|---|
| Customer sends the first message in a thread | Cleaner gets a one-time "new conversation" email; message appears in their chat/dashboard in realtime | Reply, or ignore |
| Customer requests a booking | Cleaner gets an email + a `REQUESTED` system pill in chat; booking appears on their dashboard with Confirm/Decline actions | Confirm (charges the customer) or Decline within 24h |
| Cleaner confirms | Customer's card is charged; customer gets `sendBookingConfirmedEmail` + a `CONFIRMED` pill | Cancel (refundable if ≥24h out), or wait for the job |
| Cleaner declines | Customer sees a `DECLINED`-flavoured pill (booking status `CANCELLED`) | No email fires — customer only learns from opening the thread |
| Either party cancels | Other party sees a `CANCELLED` pill; no email to either side | Nothing further on this booking |
| Cleaner uploads completion photos | Not visible to the customer until `COMPLETE` is called (photos aren't streamed live into the thread) | — |
| Cleaner marks complete | Customer gets `sendBookingCompletedEmail` and, on their dashboard, a review prompt | Leave a review |
| Customer leaves a review | Cleaner's `avg_rating`/`review_count` update automatically (DB trigger); review appears publicly on the cleaner's profile | Nothing actionable — cleaners can't respond to reviews |
| Admin approves/rejects verification | Cleaner gets an email either way; on approval, a verified badge appears on their public profile immediately | Nothing further, or resubmit (gap #1 — no way to actually do so) |
| Admin resolves a dispute | Both customer and cleaner get separate emails, each framed as won/lost from their own side | Nothing further — no appeal flow |
| Cleaner responds to a dispute | Nothing notifies the admin proactively — it just appears in the next `GET /api/admin/disputes` read | Admin can now see the response when they next open the queue |
| Customer/cleaner sends the first message in a new support thread | Admin gets a one-time email alert (`ADMIN_EMAIL`); thread appears in `/admin/messages` on next load (not live) | Reply from the unified inbox, or mark resolved |
| Admin replies in a support thread | User sees it live via Realtime if the chat is open, otherwise on next visit — no email sent | Reply, or the thread stays open until either side closes it |

---

## 9. Authentication and access control

- **Provider**: a single NextAuth Credentials provider (`src/lib/auth/config.ts:42-80`) — looks up `users` by lowercased email via the service-role admin client, compares bcrypt hash. JWT session strategy; no Supabase Auth session exists anywhere. On a successful match, also stamps `users.last_login_at` (added 2026-08-21, best-effort/non-blocking) — the only login-recency signal in the schema, backing the admin analytics "active cleaner" definition (§7).
- **Supabase RLS bridge**: since there's no Supabase Auth session, RLS's `auth.uid()` is satisfied by a custom-signed Supabase-compatible JWT minted from the NextAuth session on demand (`src/lib/supabase/authToken.ts`, `GET /api/supabase-token`) — used specifically where the browser talks to Supabase directly (Realtime chat subscriptions, and one RLS-dependent fetch on the cleaner dashboard).
- **Route gating** — `src/middleware.ts`, combining `next-auth/middleware`'s `withAuth` with the `next-intl` middleware. Order matters (more specific first):
  1. `/admin/**` → `token.role === 'ADMIN'`
  2. `/dashboard/cleaner/**` → `token.role === 'CLEANER'` (checked before the general rule below)
  3. `/dashboard/**` → any authenticated session
  4. everything else → public
  - `/api/**` routes bypass this middleware entirely; each one calls `getServerSession` itself.
  - Every protected page **also** runs its own client-side `useSession` + `router.replace('/login')` guard — this is only to avoid a flash of unauthorized content while the session resolves; the middleware is the actual boundary.
  - **Two live bugs found and fixed in this matching logic, both the same underlying mistake — matching on a raw string prefix instead of a path segment:**
    - **2026-08-21, Greek-locale bypass**: the checks above ran against the raw pathname, which for a Greek URL is locale-prefixed (`/el/dashboard`, not `/dashboard`) — `pathname.startsWith('/dashboard')` never matched, so `/el/dashboard/**` and `/el/admin/**` skipped the session/role check entirely and returned 200 to an unauthenticated request. Confirmed live via `curl` before fixing. Downstream API routes still enforce their own session checks, so this wasn't a full data leak, but it broke middleware as the sole authorization gate the project relies on. Fixed by stripping the locale prefix before matching (`stripLocalePrefix()`).
    - **2026-08-21, `/dashboard/cleaner` prefix collision**: adding the app-native cleaner-profile page at `/dashboard/cleaners/[slug]` (any authenticated role) exposed that rule 2 above was a bare `pathname.startsWith('/dashboard/cleaner')` — which also matches `/dashboard/cleaners/...` (missing the segment boundary), wrongly forcing `CLEANER`-only on a route meant for every role and 404-redirecting a `CUSTOMER` straight back to login. Same mistake existed in `(app)/layout.tsx`'s client-side `requiredRoleFor()` belt-and-suspenders guard, which sent an authorized `CUSTOMER` back to their own dashboard instead. Both fixed to `pathname === '/dashboard/cleaner' || pathname.startsWith('/dashboard/cleaner/')` (and the equivalent for `/admin`, hardened defensively even though no collision was found there).
- **Rate limiting** — in-memory, per-instance, best-effort (`middleware.ts:42-84`). Covers registration, forgot-password, resend-verification, reset-password, validate-reset-token, and the NextAuth credentials callback — 10 requests/60s per IP+path. Explicitly not reliable across a multi-instance/multi-region deployment.
- **Session shape**: `session.user = {id, email, name, role, avatar_url}`. `role` drives nearly all client-side branching — which dashboard renders, which nav links show (`Navbar.tsx`, `BottomTabBar.tsx`).
- **Post-login redirect**: `login/page.tsx` fetches the session after `signIn()` and routes by role — `CLEANER → /dashboard/cleaner`, `ADMIN → /admin`, else `safeReturnPath(searchParams.get('return')) ?? /dashboard` for a CUSTOMER, honoring a same-origin `?return=` set by e.g. `cleaners/[slug]/page.tsx` when it bounced an anonymous visitor to log in.
- **RLS as a second line of defense**: almost every API route uses the service-role admin client and enforces authorization manually (session + row-ownership checks) inside the handler — RLS mainly matters for the few paths where the browser client talks to Supabase directly. Notable RLS specifics: `cleaner_profiles` are publicly selectable only where `status = 'ACTIVE'` (plus the owner can always see their own row); `introductions`/`messages` are visible only to the thread's two parties; `payments` and `disputes` have RLS enabled with **no policies at all**, by design — they're only ever touched via the admin client.
- **Self-service account deletion** — entry point on `/dashboard/profile` (type-`DELETE`-to-confirm modal), `DELETE /api/user/me`. Anonymizes rather than hard-deletes: `users.deleted_at` is stamped and `full_name`/`email`/`phone`/`avatar_url`/`stripe_customer_id`/`password_hash` are overwritten (bookings/payments/reviews/disputes keep their FK, so the transaction trail survives under a "Deleted user" placeholder); saved `addresses` rows are deleted outright. For a `CLEANER`, `cleaner_profiles` gets the same anonymize treatment plus `status → 'SUSPENDED'` (drops out of the public `ACTIVE`-only listing/RLS), and `id_photo_url`/`selfie_photo_url`/profile photos are actually removed from storage, not just nulled. Blocked with a 409 (not deleted) while the account has: a `REQUESTED`/`CONFIRMED` booking, an `OPEN` dispute, or a booking with a `REFUND_FAILED` payment — resolve those first. `authorize()` in `config.ts` treats `deleted_at != null` the same as "no such user" (anti-enumeration, same as every other auth failure). No scheduled retention window before anonymization (e.g. a 30-day undo grace period) — runs immediately; deferred pending the actual legal retention requirement from the entity/tax advisor.

---

## 10. Edge cases and race conditions

- **Booking confirm/decline/cancel's check-then-act race is guarded twice over — this was stale here as a live gap; confirmed fixed while auditing adjacent code for the Stripe Connect payout work (2026-08-18).** `PATCH /api/bookings/[id]` validates the requested transition against the `status` it just read, then writes with `.update(...).eq('id', params.id).eq('status', booking.status)` — the second `.eq` means a losing concurrent request affects zero rows and gets a clean 409, not a clobber. `CONFIRM`'s Stripe call additionally passes `idempotencyKey: 'confirm-${booking.id}'`, so even if two requests somehow both reached `paymentIntents.create`, Stripe itself would return the same PaymentIntent to both rather than charging twice. Covered by `confirm-concurrency.integration.test.ts`.
- **Booking expiry is lazy, not scheduled.** A `REQUESTED` booking past its 24h window doesn't flip to `CANCELLED` until *something* triggers a `GET /api/bookings` (either party loading their dashboard) or a `PATCH` attempt re-checks the window. If neither party opens the app, an expired-in-spirit booking can sit as `REQUESTED` indefinitely in the database, and its payment row stays `PENDING` (never charged, since `CONFIRM` independently re-validates the window and will refuse a stale confirm with a 409).
- **A cleaner going `PAUSED`/`SUSPENDED` mid-booking triggers no automatic cancellation or customer notification.** Admin can set either state today (`/admin/users`, `PATCH /api/admin/users/[id]`), but grepping the booking flow for any `cleaner_profiles.status` check turns up none — an in-flight `REQUESTED`/`CONFIRMED` booking with that cleaner just sits there unaffected.
- **Introduction (thread) creation race is handled explicitly.** `POST /api/introductions` (`introductions/route.ts:91-115`) catches Postgres `23505` (unique-violation on `customer_id, cleaner_profile_id`) from two concurrent "start chat" clicks and re-fetches the winning row instead of erroring — one of the few places in the codebase with an explicit concurrency guard.
- **Payment-row insert failing after the booking insert succeeds is handled by explicit rollback** (`bookings/route.ts:195-198`) — the booking row is deleted if the `payments` insert fails, so a booking can't exist without a corresponding payment row. The reverse isn't possible by construction (payment insert only runs after a successful booking insert).
- **A failed refund on cancellation does alert admin and is flagged distinctly — also stale here, confirmed fixed same pass as above.** `payments.status → REFUND_FAILED` (not left `PAID`) and `sendRefundFailedAlertEmail` fires immediately, in addition to the admin cancellations ledger's badge and the manual retry endpoint (`POST /api/admin/cancellations/[id]/retry-refund`). The same pattern (status flip + admin alert + manual retry) is reused for the dispute auto-resolve refund (§6) and the cleaner payout transfer (§4) — see `refund-failure-recovery.integration.test.ts`.
- **Email failures never block the action they're attached to** — every email call site across the app is wrapped in its own try/catch with the error only logged, consistent with the "best-effort" framing in §5. There's no retry queue; a Resend outage at the exact moment of a booking confirmation means that email is simply lost.
- **Review submission race**: `POST /api/reviews` both pre-checks for an existing review and relies on a DB-level unique constraint on `booking_id` as the actual guard (`reviews/route.ts:43-51`) — two near-simultaneous submissions for the same booking would have the second one fail at the DB constraint rather than silently duplicate.
- **Duplicate dispute-response submission is blocked by status, not a lock** — `PATCH /api/cleaner/disputes/[id]` rejects if `status !== 'OPEN'`, but since resolution requires an admin action in between, this isn't a tight race window in practice.
- **Payout release has the same double-guard as booking confirm.** `releaseDuePayouts()`/`releaseBlockedPayoutsForCleaner()` (`src/lib/payouts.ts`) can both run concurrently (cron + a lazy check landing at the same moment) — the eventual `payments` update is guarded on `.eq('payout_status', row.payout_status)` (a losing write affects zero rows), and the Stripe transfer itself carries `idempotencyKey: 'payout-${booking.id}'`, so even a genuine double-attempt returns the same Transfer rather than paying twice.

---

## 11. Data model reference

Authoritative schema: `supabase/schema.sql`. TS mirror: `src/types/index.ts`.

### Enums (`schema.sql:11-16`, `types/index.ts:1-16`)
- `user_role`: `CUSTOMER | CLEANER | ADMIN`
- `cleaner_status`: `ACTIVE | PAUSED | SUSPENDED` (only `ACTIVE` is used anywhere in app code — `PAUSED`/`SUSPENDED` have no booking-flow effect, see §10)
- `service_type`: `HOUSE | APARTMENT`
- `booking_status`: `REQUESTED → CONFIRMED → COMPLETED`, or `→ CANCELLED` from either of the first two
- `payment_status`: `PENDING | PAID | REFUNDED | FAILED | REFUND_FAILED` (the last is a Stripe refund call itself erroring — see §10)
- `cleaning_type`: `STANDARD | DEEP | MOVE_IN_OUT` (`MOVE_IN_OUT` added 2026-08-19 — see `cleaner_service_offerings` below)
- `dispute_status`: `OPEN | RESOLVED`
- `dispute_resolution`: `CUSTOMER | CLEANER | UNRESOLVABLE` (who the admin ruled for; `UNRESOLVABLE` is a neutral split decision, not a finding against either party — see §7). `disputes.auto_resolved` (not an enum, a plain boolean) separately flags a `CUSTOMER` resolution that was forced by SLA timeout rather than an actual admin ruling.
- `payout_status`: `PENDING | BLOCKED | PAID | FAILED` — a cleaner's payout for one booking; see §4's payout section for what each means and `payments.cleaner_payout_eur`/`platform_fee_eur`/`payout_release_at`/`stripe_transfer_id`/`paid_out_at` for the amount/timing columns it travels with.
- `no_show_status` (added 2026-08-20): `PENDING | CONFIRMED | REJECTED` — see §11's "No-show marking" for the full flow. `no_show_flags.resolution` (`REFUND_CUSTOMER | REDIRECT_TO_CLEANER | SPLIT`) is a plain text `check` constraint, not an enum, since it's only ever set alongside `CONFIRMED`.
- `corroboration_response` (added 2026-08-20): `CORROBORATES | DISPUTES` — an *other* assigned cleaner's input on a no-show flag.
- `recurring_series_status` (added 2026-08-21): `ACTIVE | CANCELLED` — see §11's "Recurring bookings".

### Triggers — never replicate this logic in application code
- `on_review_insert` → `update_cleaner_stats()`: recomputes `avg_rating`, `review_count` (from `reviews`) and `unique_customer_count`, `total_jobs_count` (from `COMPLETED` `bookings`) on the cleaner's profile, fired on every review insert.
- `on_booking_status_change` (BEFORE UPDATE) → `on_booking_completed()`: when `status` transitions to `COMPLETED`, stamps `review_prompted_at = now()` and increments `total_jobs_count` / recomputes `unique_customer_count` on the cleaner profile.

### RLS by table
- `users`: select/update own row only.
- `addresses`: select/insert/delete own rows only (no update policy — client does delete+re-add).
- `cleaner_profiles`: public select where `status='ACTIVE'`, plus the owner can always select their own row regardless of status.
- `cleaner_service_offerings` (added 2026-08-19): same shape, joined through `cleaner_profiles` — public select where the parent profile is `ACTIVE`, plus the owning cleaner can always select their own rows. No insert/update/delete policies; writes go through the admin client (`PATCH /api/cleaner-profiles/me/offerings`).
- `introductions`, `messages`: visible only to the two thread parties.
- `reviews`: public select; no insert/update policy for anon/authenticated roles (writes go through the admin client from `/api/reviews`).
- `payments`, `disputes`, `booking_assignments`, `dispute_assignment_outcomes`, `no_show_flags`, `no_show_corroborations`, `recurring_series`, `recurring_series_skips`: RLS enabled with **no policies at all**, by design — all only ever touched via the service-role admin client, never the anon-key browser client; every API route does its own session-based authorization instead.
- `support_threads`: owner or any admin. `messages_own_thread` extends the same rule to the support-thread branch.
- `booking_assignments`, `dispute_assignment_outcomes` (added 2026-08-19): same no-policies-at-all shape as `payments`/`disputes`.

### Multi-cleaner bookings
Schema added 2026-08-19 per the deep-planning session Jack's own Todoist task called for before any building started; the booking-creation flow (stage 2 of that plan) shipped the same day. `bookings.cleaner_profile_id` and `bookings.introduction_id` are nullable — `NULL` on both is the discriminator for "this is a multi-cleaner booking" (a single-cleaner booking always has both set, completely unchanged code path). `booking_assignments` holds one row per (booking, assigned cleaner) — own `tier_rate_eur`, own `platform_fee_eur` (charged per cleaner, not once per booking — an N-cleaner job pays N × `BOOKING_FEE_EUR`), own payout tracking (`payout_status`/`cleaner_payout_eur`/etc., mirroring `payments`' columns but not yet actually processed — see the payouts gap below), own `introduction_id` (each assigned cleaner keeps their own normal 1:1 chat thread with the customer — no group-chat concept exists). `disputes.cleaner_profile_id` is nullable too: `NULL` means a whole-job dispute against a multi-cleaner booking (a dispute is always filed against the whole job, never a specific cleaner, per Jack's decision) — `dispute_assignment_outcomes` is where an admin's per-cleaner resolution/refund-percentage ruling would live, one row per assigned cleaner; **not wired up yet** (see gaps below).

**Creating one**: `/cleaners/book-team?slugs=a,b,c` (reached from a "Book multiple cleaners" select-mode toggle on `/cleaners` — `CleanerCard` becomes a checkbox instead of a profile link while active). The tier/add-on picker only offers a `cleaning_type` every selected cleaner has priced (`cleaner_service_offerings` intersection) — add-ons aren't offered on multi-cleaner bookings at all yet (open product question: how would an add-on's fee attribute across N cleaners). A new availability check (`isCleanerAvailableAt` in `src/lib/availability.ts`) rejects any selected cleaner whose weekly hours don't cover the requested slot — **new for multi-cleaner only**; the ordinary single-cleaner booking flow still has zero availability enforcement, a deliberate inconsistency, not an oversight. `POST /api/bookings/multi` (a sibling to, never touching, `POST /api/bookings`) re-validates everything server-side, finds-or-creates each cleaner's own `introductions` thread (reusing the same find-or-create logic as `POST /api/introductions`), and inserts one `bookings` row + N `booking_assignments` rows + one combined `payments` row in a single request.

**Everything downstream of creation reuses the single-cleaner machinery, widened to recognize `booking_assignments`**: `PATCH /api/bookings/[id]` (confirm/decline/cancel/complete) now authorizes any assignee the same way it authorizes the single `cleaner_profile_id`, and fans system messages/cancel-emails out to every assigned cleaner's own thread instead of one. `GET /api/bookings` (CLEANER role) merges bookings found via `cleaner_profile_id` with bookings found via `booking_assignments`, so a cleaner's dashboard shows both kinds together. The Stripe charge itself is unchanged — still one PaymentIntent for the combined total, charged at CONFIRM by whichever assigned cleaner acts first, exactly like the single-cleaner flow.

**Payouts** (added 2026-08-19, stage 3): `src/lib/payouts.ts` now has a second candidate path alongside its original `payments`-keyed one — `releaseDuePayouts`/`releaseBlockedPayoutsForCleaner` also scan `booking_assignments` for multi-cleaner bookings and release each assignment independently. Readiness (is the dispute window/hold clear) is still governed by the booking's one `disputes` row, same as single-cleaner; the refund *percentage* that reduces the payout is looked up per-cleaner from `dispute_assignment_outcomes` instead of `disputes.refund_percentage` (0% if that dispute didn't rule against this specific cleaner, or if there's no dispute at all). `no_show` unconditionally zeroes a payout regardless of any dispute outcome. Each assignment fires its own Stripe transfer, idempotency-keyed per `(booking, cleaner)` — `payout-${booking.id}-${cleanerProfileId}` — but sharing the same `transfer_group` as the booking's other assignments' transfers, since `transfer_group` exists precisely to link multiple transfers back to one originating charge. Verified live: a forced-COMPLETED multi-cleaner test booking correctly went `BLOCKED` for a cleaner with no Stripe Connect account and correctly reached a real (test-mode) Stripe transfer call for one with Connect enabled, computing the right amount before Stripe itself rejected it (`balance_insufficient` — expected, since the test booking's charge was faked rather than real).

**Dispute resolution** (added 2026-08-19, stage 4): `PATCH /api/admin/disputes/[id]` branches on `dispute.cleaner_profile_id` — single-cleaner disputes are byte-for-byte unchanged. A multi-cleaner dispute instead accepts one resolution + refund percentage per assigned cleaner (`assignments[]`), written to `dispute_assignment_outcomes` before the dispute is marked resolved. The customer-facing refund is always exactly one Stripe refund (one PaymentIntent regardless of cleaner count), sized to the sum of each cleaner's own share (`tier_rate_eur × duration_hours + platform_fee_eur`) × their own ruling — a mixed ruling (one cleaner favored the customer, another the cleaner) still resolves to one correctly-blended refund. Each cleaner is emailed their own true personal outcome, not a shared one. The admin UI (`/admin/disputes`) shows one resolution row per assigned cleaner when applicable, defaulting to Cleaner/0% until actively changed.

**No-show marking — revised 2026-08-20**: the 2026-08-19 v1 (an admin-only checkbox, no customer/cleaner involvement) was rebuilt to match the workflow decided immediately after it shipped: customer-initiated → other assignee(s) corroborate/dispute → the flagged cleaner can contest → admin rules with full context, then reallocates the flagged cleaner's forfeited share. New tables `no_show_flags` (one per flagged assignment, unique on `assignment_id`) and `no_show_corroborations` (one per *other* assignee's response — the flagged cleaner's own input is `no_show_flags.cleaner_response` instead).

Flow: `POST /api/no-show-flags` (customer, from a completed multi-cleaner booking's dashboard card, same 24h-from-`completed_at` filing window as disputes) creates a `PENDING` flag with a 24h `resolve_by` SLA and emails four parties — admin, the filing customer (confirmation), the flagged cleaner (contest prompt), and every *other* assigned cleaner on that job (corroboration request). The flagged cleaner responds via `POST /api/no-show-flags/[id]/contest`; other assignees respond via `POST /api/no-show-flags/[id]/corroborate` (`CORROBORATES`/`DISPUTES` + optional note, upsertable while still `PENDING`). Both are visible as banners on the cleaner dashboard's booking card.

Admin resolves at `/admin/team-bookings` (nav tab "No-show reports", `GET`/`PATCH /api/admin/no-show-flags[/[id]]`) — a queue of flags (not a browse-every-completed-booking list like the old page), showing the claim, the flagged cleaner's contest, and every other assignee's corroboration before ruling. **Reject**: closes the flag, `booking_assignments.no_show` stays false, nothing moves. **Confirm**: sets `no_show = true` on that assignment (same guard as before — 409s once `payout_status` is past `PENDING`/`BLOCKED`, no clawback mechanism exists), which alone zeroes that cleaner's payout via the existing unconditional check in `src/lib/payouts.ts`. The admin then separately decides where the *customer's* money for that forfeited share goes — same `shareEur` formula and Stripe refund mechanics as `PATCH /api/admin/disputes/[id]`'s multi-cleaner branch, deliberately reused rather than reinvented: `REFUND_CUSTOMER` (full Stripe refund), `REDIRECT_TO_CLEANER` (a bonus transfer to another assignee on the same job who covered it — new, no prior mechanism moved money between two cleaners), or `SPLIT` (a percentage refunded, the remainder either redirected or left with the platform). A failed refund/transfer doesn't block the ruling — logged and admin-alerted, same philosophy as every other refund path in the app.

`src/lib/payouts.ts` gates each assignment's payout on there being no `PENDING` `no_show_flags` row for it, the same way an `OPEN` dispute gates readiness — otherwise a flag filed inside the 24h payout hold could lose the race and pay out before anyone reviews it.

**Verified live 2026-08-20**: filed a flag as the customer against one of two real assigned cleaners on a force-completed test booking, corroborated as the other assignee, contested as the flagged cleaner, then resolved as admin with `REDIRECT_TO_CLEANER` — confirmed a real (test-mode) Stripe transfer fired for the exact expected amount (`tr_1U6c0BRiJSDnQ5uy9GgrUN5e`, €28.50) to the covering cleaner's Connect account, idempotency-keyed `noshow-redirect-{flagId}`, and `booking_assignments.no_show` flipped correctly on the flagged assignment. Found and fixed one real bug in the process: `no_show_flags` comes back from Supabase as a single object, not an array, since `assignment_id` is unique — the customer/cleaner dashboards initially assumed an array (indexing `[0]`) and silently never reflected a filed flag until fixed.

**Known gaps, not yet built**: Any assigned cleaner marking COMPLETE completes the whole booking for everyone — there's no partial-completion UX yet even though the schema supports per-assignment tracking.

**Customer dashboard display — fixed 2026-08-20**: `GET /api/bookings` (CUSTOMER role) only ever selected `cleaner_profiles`, which is null on a multi-cleaner booking by design (§11's own discriminator) — the dashboard's booking card and detail modal rendered a bare "—" with no cleaner names at all, and the payment breakdown used singular "Cleaner's rate"/"Booking fee" labels regardless. Fixed by also selecting `booking_assignments ( cleaner_profile_id, cleaner_profiles (...) )` in that query and joining the assigned cleaners' names ("Jack Rowsell & Natasha") wherever the single `cleaner_profiles` is null; the payment breakdown now switches to the plural labels ("Cleaners' rates" / "Booking fees (N)") already used on the booking form itself when `cleanerCount > 1`. Found during the DEEP DIVE usability review of the multi-cleaner booking system.

**Cleaner-side team awareness — fixed 2026-08-20**: same review found a cleaner assigned to a multi-cleaner job had zero indication anyone else was involved — their booking card looked identical to an ordinary solo booking. `GET /api/bookings` (CLEANER role)'s `booking_assignments` select didn't include the other assignees' names at all. Fixed by nesting `cleaner_profiles ( id, display_name )` there and showing a "Team job — also working with {names}" line (filtering out the viewing cleaner's own assignment) on both the request and confirmed cards. **Confirmed but not changed, worth a product decision**: confirming/declining is still booking-level, not per-assignment — when either assigned cleaner confirms, `PATCH /api/bookings/[id]` flips the one shared `bookings.status` to CONFIRMED for both (see this section's "Everything downstream of creation" note on why); there's no independent per-cleaner accept step. Verified live: Jack confirming immediately showed the job as CONFIRMED on Natasha's dashboard too, before she'd taken any action herself.

### Recurring bookings
Added 2026-08-21 — a standing "clean every 2 weeks" arrangement between one customer and one cleaner, green-lit 2026-08-18 with the billing model and skip/reopen behavior already decided. Deliberately **not** a template that generates bookings through some parallel lifecycle: every occurrence is a completely ordinary `bookings` row, stamped `recurring_series_id`, reusing the existing `REQUESTED → CONFIRMED → COMPLETED` lifecycle, 24h dispute window, and payout job unchanged — the only new machinery is what *creates* those rows and what *charges* for them.

**`recurring_series`** holds the recurring shape itself: cadence (`day_of_week` 0–6 matching JS `Date.getDay()`, `start_time`, `anchor_date` — every later occurrence is `anchor_date + 14×n` days), the job details and saved `payment_method_id`, and a **price snapshot** (`tier_rate_eur`/`platform_fee_eur`/`addon_total_eur`/`amount_eur`) taken once at series creation and reused unchanged for every occurrence — same "never rewrite history" philosophy as `payments`' own snapshot columns, just extended forward instead of only back: a cleaner's later rate change never re-prices a standing arrangement mid-series. A cleaner who wants to change the price cancels the series; the customer sets up a new one. `bookings.recurring_series_id` is nullable — `NULL` (the overwhelming majority) is an ordinary one-off booking, completely unaffected.

**Slot-blocking is recurring-only, a deliberate scope decision**: this app has **no general double-booking prevention for any booking type** — a customer can still double-book the same cleaner for overlapping one-off slots today, nothing stops it, and this work didn't change that. Only recurring series check for conflicts against each other (`POST /api/bookings/recurring` rejects a new series if the same cleaner already has an ACTIVE series on the same weekday with an overlapping time window) — one-off bookings aren't checked against series or each other.

**Creating one**: a "Repeat every 2 weeks" checkbox on the booking form (`BookingFormModal`) — when checked, submits to `POST /api/bookings/recurring` instead of `POST /api/bookings`. That route does everything the plain one does (same validation, same tier/add-on pricing resolution against `cleaner_service_offerings`) plus the slot-block check above and the existing `isCleanerAvailableAt` weekly-hours check (`src/lib/availability.ts`, same one multi-cleaner bookings already use), then inserts the `recurring_series` row and occurrence #1 as an ordinary `REQUESTED` booking in the same request. Occurrence #1 goes through the **normal** cleaner confirm/decline and off-session charge (`PATCH /api/bookings/[id]`, byte-for-byte unchanged) — nothing about accepting the first cleaning is different from a one-off booking.

**Auto-confirm after #1, via a new daily cron**: `GET /api/cron/charge-recurring` (registered in `vercel.json`, `0 4 * * *` — the next free slot in the existing 03:00/03:15/03:30/03:45 stagger, same once-daily Vercel Hobby-plan ceiling as every other cron here). For each `ACTIVE` series whose occurrence #1 is `CONFIRMED` (that's what "the cleaner accepted the relationship" means operationally), it finds the next unhandled occurrence date (`src/lib/recurringBookings.ts`'s `computeNextOccurrenceDate` — shared with the skip route below so the two can never disagree about which date is next) and, if it's within 72h and hasn't been skipped, inserts a new `bookings` row **already `CONFIRMED`** (no fresh accept needed — the standing arrangement *is* the ongoing consent) and immediately off-session charges the saved card, mirroring the exact `stripe.paymentIntents.create({ off_session: true, confirm: true })` shape the `CONFIRM` branch already uses. If occurrence #1 was instead declined/cancelled, the cron auto-cancels the orphaned series (there's no accepted relationship to continue).

**A charge failure deliberately does not unwind the booking** — same philosophy as every other payment-side failure in this app (a failed cancellation refund, a failed payout): the `CONFIRMED` booking stands, `payments.status` goes `FAILED`, and an admin alert fires. This is a real, deliberate gap worth knowing about: the cleaner is still expecting to show up for a booking nothing was actually collected for, and nothing here retries the charge automatically — needs a human to either retry it or cancel the booking. Not addressed by this pass.

**Skipping one occurrence** — `POST /api/recurring-series/[id]/skip`, either party, always targets *the next* occurrence (no client-supplied date; same `computeNextOccurrenceDate` the cron uses) and only makes sense *before* that occurrence has been charged/created — inserts a `recurring_series_skips` row, and the cron simply never creates a `bookings` row for that date, which **is** "the slot reopens": there was never a static weekly block to begin with, just "does an unhandled occurrence exist for this date," so a skipped date is indistinguishable from an ordinary open slot. Skipping/cancelling an occurrence that's *already* a real booking (already charged) is just the existing per-booking cancel action — full refund if ≥24h out, zero effect on the rest of the series, since every occurrence is an independent row.

**Ending the series** — `PATCH /api/recurring-series/[id]`, either party, sets `status: 'CANCELLED'`. The cron simply skips cancelled series going forward; it does **not** reach back and cancel an already-created upcoming occurrence — that's handled separately via the normal per-booking cancel action if the customer wants that too.

**UI**: a "Recurring — every 2 weeks" badge plus "Skip next clean" / "Cancel recurring series" actions render on any booking card belonging to an `ACTIVE` series, on both the customer and cleaner dashboards — deliberately on every occurrence's card rather than a single dedicated series view (no series-level list page exists yet), so the same actions can currently appear more than once if multiple occurrences from one series are visible at once. A minor polish gap, not a correctness one.

**Live-verified 2026-08-21 (partial)**: created a real series (Sasha → Jack, bi-weekly, €20.50), Jack confirmed occurrence #1, and the off-session Stripe charge succeeded (`payments.status = PAID`) — the create → confirm #1 → charge path is confirmed working end-to-end in production. Cron-spawn of occurrence #2, skip, and cancel-series are code-reviewed but not yet independently live-verified; occurrence #2's auto-create/auto-charge will confirm itself via the real daily cron run (`0 4 * * *`) rather than a manual trigger, since manually invoking the cron would require handling `CRON_SECRET`, which the credential-handling rules for this session route around. Follow-up: confirm occurrence #2 landed as `CONFIRMED`/`PAID` after the next cron run, then test skip (on occurrence #3) and cancel-series.

**Incidental fix, 2026-08-21**: found and fixed a live production bug while testing this — `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` was never set in Vercel (only `STRIPE_SECRET_KEY` was), so the booking payment element crashed on mount and **no booking of any kind, recurring or one-off, could be created** on production. Added the missing env var and redeployed; confirmed the payment form renders and charges correctly afterward.

### `schema.sql` drift — resolved
A previous pass (2026-08-17) found `verification_tokens` and four `cleaner_profiles` columns (`cities`, `cleaner_type`, `gender`, `is_mock`) missing from the checked-in `schema.sql` despite being live in production. **Verified 2026-08-19: all five are now present in `schema.sql`** (`verification_tokens` table, `cleaner_profiles.cities`/`cleaner_type`/`gender`/`is_mock`) — the drift this section used to document has been fixed since. Noting the resolution here rather than deleting the section outright, since it's evidence the file is worth periodically re-checking against the live schema rather than trusted blindly.
